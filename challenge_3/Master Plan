Master Plan

Homepage
- Checkout Button
	- on click, takes you to the first form
	- inits a new record in the database -- all values null

- F1 - login
	- collects name, email, password
	- next button
		- updates relevant records

- F2 - address 
	- line1, line2, city, state, zip, phone num
	- next button
		-updates relevant records

- F3 - cc info
	- cc#, expiration, cvv, billing zip
	- next button
		- updates relevant records

- Confirmation page
	- summarize data
	- purchase button
	- return to homepage

mysql server start

	-install mysql
	-start mysql server (mysql .server start)
	-install a module that will allow us to connect to the mysql server (specific to sql module (db?))
	-connect obj to connect to the mysql server

mongo -- require db in 


Next:
	- connect app and index.js 

		-- connect server to index.js, call functions in index.js on get/post requests
		- when checkout is clicked a new entry is created
		- when next is clicked, the entry is updated